# Doctor Clinic API Documentation / توثيق عيادة الطبيب

هذا المستند يشرح كيفية استخدام الـ API لتنفيذ ميزات "عيادة الطبيب" بناءً على الهيكلية الحالية للنظام.

---

## 1. معلومات المريض (Patient Info)

لعرض بيانات المريض (الاسم، العمر، الوزن، الطول، رقم الهاتف):
- **الرابط:** `GET /api/patients/{id}`
- **الاستجابة:** تحتوي على بيانات موديل `Patient` المرتبط بجداول `users` و `subscribed_users`.

---

## 2. جدول الحصص (Food Exchange Table)

في هذا النظام، "جدول الحصص" هو `MealCategory`. كل فئة تحتوي على الماكروز (بروتين، دهون، كربوهيدرات) الخاصة بها.
- **الرابط:** `GET /api/meal-categories`
- **البيانات المرجعية:**
    - `protein`: البروتين في الحصة الواحدة.
    - `fat`: الدهون في الحصة الواحدة.
    - `carbohydrates`: الكربوهيدرات في الحصة الواحدة.
    - `energy`: السعرات الحرارية.

---

## 3. إنشاء الحمية (Diet Planning)

يتم إنشاء الحمية عبر ربط الوجبات بخطة غذائية:
- **الخطوة الأولى: إنشاء خطة (Diet Plan):**
    - `POST /api/diet-plans` (العنوان، الوصف، السعرات اليومية، المدة).
- **الخطوة الثانية: إضافة وجبات (Meals):**
    - `POST /api/meals`
    - **الحقول:** `diet_plan_id`, `meal_type` (Breakfast, Lunch, Dinner), `meal_name`, `carbo`, `protin`, `fat`, `energy`.
    - *ملاحظة:* هنا يقوم الطبيب باختيار "الحصة" من جدول الحصص، ويقوم الكود (أو الواجهة) بضرب قيم الحصة في عدد الحصص وحفظها في حقول `carbo`, `protin` الخ.

---

## 4. الملاحظات والتعليمات (Diet Notes)

لكتابة ملاحظات للطبيب (مثل "اشرب الماء كل 3 ساعات"):
- **الرابط:** `POST /api/diet-notes`
- **الحقول:** `user_id`, `doctor_id`, `note`.

---

## 5. الدردشة (Chat)

التواصل المباشر بين الطبيب والمريض:
- **إرسال رسالة:** `POST /api/messages`
- **الحقول:** `doctor_id`, `user_id`, `message`, `sender_type` (doctor/patient).
- **عرض المحادثة:** `GET /api/messages?with_user_id={id}`

---

## 6. حسابات الجسم والفحوصات (Metrics & Tests)

- **حسابات الجسم:** `GET /api/weekly-calculations` (يحتوي على القياسات: الخصر، البطن، الصدر، الخ).
- **الفحوصات الطبية:** `GET /api/medical-tests` (عرض الصور والملفات المرفوعة من المريض).
- **السجلات التاريخية:** `GET /api/measurements` (تتبع الوزن عبر الوقت).

---

## 7. الحاسبات المتقدمة (Specialized Calculations)

هذا الرابط مخصص للأطباء لحساب الاحتياجات الدقيقة للمريض بناءً على المعادلات المعتمدة:
- **الرابط:** `POST /api/calculations/nutrition`
- **الجسم (Body):**
```json
{
    "weight": 85,
    "height": 180,
    "age": 30,
    "gender": "male",
    "activity_level": "moderate",
    "goal": "lose",
    "save": true
}
```
- **معاملات النشاط (`activity_level`):** `sedentary` (1.2), `low` (1.3), `moderate` (1.5), `active` (1.7), `very_active` (1.9).
- **الأهداف (`goal`):** `maintain`, `lose` (-500), `gain` (+500).
- **ملاحظة:** يتم إضافة 10% تلقائياً كأثر حراري للطعام (TEF).
- **الاستجابة:** تعطي السعرات الكلية وتوزيع الماكروز (50% كربوهيدرات، 20% بروتين، 30% دهون) بالجرام.

---

## 8. مراجع الطبيب (Medical Files)

لرفع مراجع تغذوية أو ملفات تعليمية للأطباء:
- **الرابط:** `GET /api/medical-files`
- **الرابط:** `POST /api/medical-files` (للمسؤول أو الطبيب لرفع مراجع).

---

> [!NOTE]
> جميع الروابط تتطلب هيدر `Authorization: Bearer {token}`.
